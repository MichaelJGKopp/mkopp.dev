Docker Container Order of Keys


1. Identität & Quelle: Was ist der Service?

    - image oder build
    - container_name

2. Laufzeit & Ausführung: Wie wird der Container konfiguriert und gestartet?

    - command
    - entrypoint
    - environment
    - secrets

3. Netzwerk & Verbindungen: Wie kommuniziert der Service?

    - ports
    - networks
    - depends_on

4. Speicher & Daten: Wo werden die Daten gespeichert?

    - volumes

5. Metadaten & Orchestrierung: Zusätzliche Konfiguration für andere Tools.

    - labels

6. Lebenszyklus & Zustand: Wie wird der Container verwaltet?

    - healthcheck
    - restart
    - deploy